import { devnet as solanadevAddressesMasaToken } from "@masa-finance/masa-token-solana/addresses.json";

import type { Addresses } from "../../interface";

export const solanadev: Addresses = {
  tokens: {
    MASA: solanadevAddressesMasaToken.masatoken,
  },
};
