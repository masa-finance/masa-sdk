import { scrolltest as scrollSepoliaAddresses } from "@masa-finance/masa-contracts-identity/addresses.json";

import type { Addresses } from "../../interface";

const { SoulboundIdentity: SoulboundIdentityAddress } = scrollSepoliaAddresses;

export const scrollsepolia: Addresses = {
  SoulboundIdentityAddress,
};
